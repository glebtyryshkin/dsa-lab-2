# dsa-lab-2

Лабораторная рабта №1: Flask + PostgreSQL + Docker / Docker Compose.

## Запуск

```bash
docker compose up --build
curl http://localhost:8000/hello
```

Останвка:

```bash
docker compose down
```

Полная очитка с томами:

```bash
docker compose down -v
```
